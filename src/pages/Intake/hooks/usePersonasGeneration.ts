
import { usePersonasManager } from "./personas/usePersonasManager";

// Re-export the hook with the same name to maintain backward compatibility
export const usePersonasGeneration = usePersonasManager;
